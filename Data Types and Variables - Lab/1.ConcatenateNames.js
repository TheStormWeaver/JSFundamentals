/*
Write a function which receives two names as string parameters and a delimiter. Print the names joined by
the delimiter.
*/
function solve(firstname, secondname, delimiter) {
  console.log(`${firstname}${delimiter}${secondname}`)
}
solve("Jhon","Smith","->");
